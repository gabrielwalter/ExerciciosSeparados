#include <stdio.h>

int main() {
    int hora, minuto;
    printf("Que horas são? (formato HH MM): ");
    scanf("%d %d", &hora, &minuto);

    printf("É hora de comer um doce!\n");
    return 0;
}